<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Design.PreviewWith>
            <Border Padding="20 20 20 120" Background="{DynamicResource main_background}">
                <StackPanel Spacing="10" Width="160">
                    <ComboBox PlaceholderText="Select an item" Width="160">
                        <ComboBoxItem IsSelected="True">123</ComboBoxItem>
                        <ComboBoxItem>231</ComboBoxItem>
                        <ComboBoxItem>312</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Border>
    </Design.PreviewWith>
  
  <Style Selector="ComboBoxItem">
  
  <!--
    Стиль для элемента выпадающего списка (ComboBoxItem).
    Определяет внешний вид и визуальные состояния: наведение, нажатие, выбор, отключение.
  -->

  <!--  Базовые свойства элемента  -->
  <Setter Property="Foreground" Value="{DynamicResource secondary_text}" />
  <Setter Property="Background" Value="{DynamicResource secondary_background}" />   <!--  Исправлено: исходный ComboBoxItemBackground, но оставляем как есть  -->
  <Setter Property="BorderThickness" Value="1 0 1 0"/>
  <Setter Property="BorderBrush" Value="{DynamicResource border}"/>
  <Setter Property="Padding" Value="5 7 0 7 "/>
  <Setter Property="HorizontalContentAlignment" Value="Stretch" />
  

  <!--  Шаблон элемента – просто ContentPresenter, который отображает контент  -->
  <Setter Property="Template">
    <ControlTemplate>
      <Panel>
      <ContentPresenter Name="PART_ContentPresenter"
                        Foreground="{TemplateBinding Foreground}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        ContentTemplate="{TemplateBinding ContentTemplate}"
                        Content="{TemplateBinding Content}"
                        HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"
                        VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"
                        Padding="{TemplateBinding Padding}" />
              <Border x:Name="SelectedOverlay"
                Background="{DynamicResource primary}"
                Opacity="0"
                IsHitTestVisible="False"/>
      
              <!-- Галочка справа -->
              <Viewbox x:Name="CheckMark"
                       Width="10" Height="10"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Margin="0 0 10 0"
                       ClipToBounds="False"
                       IsVisible="False">
                <Path StrokeLineCap="Round"
                      StrokeJoin="Round"
                      StrokeThickness="3"
                      Stroke="{DynamicResource primary}"
                      Data="{DynamicResource Check}" />
              </Viewbox>
      </Panel>
    </ControlTemplate>
  </Setter>
  
  <!-- последний элемент -->
  <Style Selector="^:nth-last-child(1) /template/ ContentPresenter">
    <Setter Property="BorderThickness" Value="1 0 1 1"/>
    <Setter Property="CornerRadius" Value="0 0 5 5"/>
  </Style>
  
  <!--  Состояние: наведение курсора (PointerOver)  -->
  <Style Selector="^:pointerover /template/ ContentPresenter">
    <Setter Property="Background" Value="{DynamicResource secondary_background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource border}" />
    <Setter Property="Foreground" Value="{DynamicResource main_text}"/>
  </Style>

  <!--  Состояние: отключённый элемент (Disabled)  -->
  <Style Selector="^:disabled /template/ ContentPresenter">
    <Setter Property="Background" Value="{DynamicResource secondary_background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource border}" />
    <Setter Property="Foreground" Value="{DynamicResource muted_text}" />
  </Style>

  <!--  Состояние: нажатие (Pressed)  -->
  <Style Selector="^:pressed /template/ ContentPresenter">
    <Setter Property="Background" Value="{DynamicResource secondary_background}" />
    <Setter Property="BorderBrush" Value="{DynamicResource border}" />
    <Setter Property="Foreground" Value="{DynamicResource main_text}" />
  </Style>

  <!--  Состояние: выбранный элемент (Selected)  -->
  <Style Selector="^:selected">
    <Style Selector="^ /template/ Border#SelectedOverlay">
      <Setter Property="Opacity" Value="0.1" />
    </Style>
    <Style Selector="^ /template/ Viewbox#CheckMark">
      <Setter Property="IsVisible" Value="True" />
    </Style>
    <!--  Базовое состояние выбранного элемента  -->
    <Style Selector="^ /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource secondary_background}" />
      <Setter Property="BorderBrush" Value="{DynamicResource border}" />
      <Setter Property="Foreground" Value="{DynamicResource primary}" />
    </Style>

    <!--  Выбранный + отключённый  -->
    <Style Selector="^:disabled /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource secondary_background}" />
      <Setter Property="BorderBrush" Value="{DynamicResource border}" />
      <Setter Property="Foreground" Value="{DynamicResource primary}" />
    </Style>

    <!--  Выбранный + наведение  -->
    <Style Selector="^:pointerover /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource secondary_background}" />
      <Setter Property="BorderBrush" Value="{DynamicResource border}" />
      <Setter Property="Foreground" Value="{DynamicResource primary}" />
    </Style>

    <!--  Выбранный + нажатие  -->
    <Style Selector="^:pressed /template/ ContentPresenter">
      <Setter Property="Background" Value="{DynamicResource secondary_background}" />
      <Setter Property="BorderBrush" Value="{DynamicResource border}" />
      <Setter Property="Foreground" Value="{DynamicResource primary}" />
    </Style>
  </Style>
</Style>
  
</Styles>
